# Dependencies
node_modules/
bridge/node_modules/

# Build output
dist/
bridge/dist/
src-tauri/target/
target/

# Tauri
src-tauri/gen/

# Local config / secrets — NEVER commit these
.env
.env.*
*.local
auth.json
settings.json
models.json
*.key
*.pem
*.p12

# User-local prime-agent data
.prime/

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Verification artifacts
verify-shots/
verify-dom.cjs
verify-cdp.cjs
verify/e2e-report.json
verify/e2e-evidence.txt
verify/updater-report.json
verify/evaluator/

# Updater test artifacts (regenerated by scripts)
scripts/update-binary.bin
scripts/update.sig
scripts/update-binary.bin.sig

# Staged runtime bundle (regenerated by scripts/bundle.mjs — large)
resources/daemon/dist/
resources/bridge/dist/
resources/node_modules/
resources/.bundle-manifest.json

# Build sandbox for in-place daemon rebuild (--rebuild-daemon)
resources/.daemon-build-sandbox/

# Stray artifacts from native tooling on Windows (NUL device, temp copy dirs)
nul
tmp-nm-copy/
prime-agent-ref

# Large binaries — downloaded by scripts/bundle.mjs, not tracked
resources/node/node-v24.18.0-win-x64/node.exe
resources/node/node.zip

# Gauntlet runtime state (transient, per-piece breadcrumbs)
evaluator-state.json
generator-state.json

# Smoke test screenshots
verify/screenshots/
