{
  "piece": "Onboarding Wizard",
  "generator": "5402881a-fac7-4df5-ab73-ae62f102698c",
  "sprint": 1,
  "timeline": [
    { "t": "start", "note": "Read role docs, loop protocol, and all context files (FirstRunBanner, GeneralPanel, onboarding.css, ChatView, AppState, design core/tokens, ipc client/contract, useModels, ModelSelector, SettingsView, App.tsx)." },
    { "t": "design", "note": "Chose new OnboardingWizard.tsx over in-place refactor; kept FirstRunBanner.tsx as re-export home for useOnboardingStatus + FirstRunBanner alias + new clearOnboardingDismissed helper. Avoided circular import by keeping useOnboardingStatus in FirstRunBanner and importing its type into the wizard." },
    { "t": "build", "note": "Created OnboardingWizard.tsx with 4 steps (welcome/provider/model/firstPrompt), progress rail, Skip at every step + footer Esc-to-dismiss, reuses setup prop from useOnboardingStatus, uses useConnectionState + useModels for model step. Wired ChatView to OnboardingWizard. Added 'Run onboarding again' button in GeneralPanel (clears dismiss flag + setView('chat')). Added wizard CSS to onboarding.css." },
    { "t": "fix", "note": "tsc TS6133: removed unused useIpc import and unused liveReady prop in ProviderStep. tsc clean after." },
    { "t": "verify", "note": "tsc --noEmit exit 0. vitest run 6 files / 86 tests pass. npm run build succeeds. Dev server on :1420 serves HTTP 200; OnboardingWizard/GeneralPanel/ChatView/FirstRunBanner modules transform with no vite errors." }
  ],
  "gates": { "tsc": "pass", "vitest": "pass (86/86)", "build": "pass", "dev_server": "pass (HTTP 200, no transform errors)" },
  "preserved": ["useOnboardingStatus export", "FirstRunBanner alias export", "onSetupProviders/onStartChat callback pattern", "IPC contract types unchanged", "no new npm deps"]
}
